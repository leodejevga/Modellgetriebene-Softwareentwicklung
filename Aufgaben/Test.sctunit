testclass Test for statechart Aufgaben {
	
	@Test
	operation test() {
		enter
//		1) bezieht sich nur auf den anfangszustand... muss aber für alle mögliche zustände geprüft werden also ob es immer stimmt.
		assert active (Aufgaben.H.Green) != active (Aufgaben.N.Green)
		exit
	}
}
